/*
8) Faça um programa que preencha um vetor com 10 números reais. 
Em seguida, calcule e mostre na tela a quantidade de números negativos e a soma dos números positivos desse vetor.
*/

#include <stdio.h>

int main(){

    int vetor[10];
    int soma;
    int count;

    for (int i = 0; i < 10; i++)
    {
        printf('Digite o numero para a posicao %d: ', i);
        
    }
    

    return 0; // retorno da funcao
}